import React, { FC } from "react";
import W from "../../styles/wrappers";

interface Props {}

const SelectedMovie: FC<Props> = () => {
	return <W.Main>Selected movie page</W.Main>;
};

export default SelectedMovie;
